import React from 'react';

export default class ErrorBoundary {
  // state = { hasError: false };

  // componentDidCatch(error, info) {
  //   // Display fallback UI
  //   this.setState({ hasError: true });
  // }

  render() {
    // if (this.state.hasError) {
    return <h1>ajdhakdhadksja</h1>;
    // }
  }
}
